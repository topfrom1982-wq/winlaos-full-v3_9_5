services:
  - type: web
    name: winlaos-register-bridge
    env: node
    rootDir: bridge
    buildCommand: npm install
    startCommand: node winlaos-register-bridge.js
    plan: free
    region: singapore
